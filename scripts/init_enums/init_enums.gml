enum SYMMETRY {
	X,
	T,
	I,
	L,
	Z,
	R,
	MAX
}

enum SIDE {
	TOP,
	LEFT,
	BOTTOM,
	RIGHT,
	MAX
}

enum CELL_STATE {
	UNCHECKED,
	IN_STACK,
	CHECKED,
	TILED,
	MAX
}

enum WFC_STATE {
	IDLE,
	BEGIN_STEP,
	PROPAGATION,
	END_STEP,
	MAX
}